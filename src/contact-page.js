import {mainContent, navBar, titleText, imgContainer} from "./home-page";
const contactPage = () => {
    mainContent.appendChild(navBar);

    mainContent.appendChild(titleText);

    mainContent.appendChild(imgContainer);
    
}
export {contactPage};